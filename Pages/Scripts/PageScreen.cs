using UnityEngine;

namespace UltimateFootballSystem.Pages.Scripts
{
    public class PageScreen : MonoBehaviour
    {
        [SerializeField]
        public string route;
    
        [SerializeField]
        public string title;
    
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
